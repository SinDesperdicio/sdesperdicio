version: '2'
services:
  sindesperdicio-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/SinDesperdicio/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=SinDesperdicio
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
